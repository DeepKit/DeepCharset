program BasicEncodingTest; { CONSOLE} uses System.SysUtils, System.Classes, System.IOUtils; begin try WriteLn('????'); if not DirectoryExists('from') then CreateDir('from'); if not DirectoryExists('to') then CreateDir('to'); if not DirectoryExists('back') then CreateDir('back'); with TStringList.Create do try Add('UTF-8????'); Add('?????'); SaveToFile('from\utf8.txt', TEncoding.UTF8); SaveToFile('to\utf16.txt', TEncoding.Unicode); SaveToFile('back\utf8_back.txt', TEncoding.UTF8); with TStreamWriter.Create('tests.md') do try WriteLine('# ??????'); WriteLine('????'); finally Free; end; finally Free; end; ReadLn; except on E:Exception do begin WriteLn(E.Message); ReadLn; end; end; end.
