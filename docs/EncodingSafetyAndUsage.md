# DeepCharset 编码检测与转换的安全使用与适用范围

> 目标：说明本工具在编码检�?转换上的**适用场景、局限性与推荐用法**，帮助用户在批量处理文本文件时，在效率与安全之间取得平衡�?

## 1. 编码检测的本质与局�?

- 文本编码检测本质上�?*启发�?+ 统计**过程，而不是绝对准确的数学证明�?
- 即便使用了多种特征（字节分布、合法序列比例、语言特征等），在以下场景中，任何检测器�?*无法保证 100% 准确**�?
  - 文本极短（例如只有几行或几十个字节）�?
  - 文本内容高度单一（只有数�?ASCII 字符）�?
  - 文件已损坏、截断或混入二进制内容�?
  - 同一文件中混合使用多种编码�?
- 因此�?
  - 自动检测结果应被视�?*“有依据的猜测�?*，而非绝对结论�?
  - 在重�?生产数据上，必须结合**预览 + 备份 + 渐进验证**来使用转换功能�?

## 2. 强推荐使用的安全场景

以下场景�?DeepCharset 最推荐、也是测试覆盖最充分的使用方式：

1. **大批量、来源相对一致的文本文件**
   - 例如：一整批来自同一系统/供应�?区域的日志、配置、脚本、文档�?
   - 特征�?
     - 文件编码在“UTF-8 系列 / GBK / Big5 / 某一固定 codepage”中少数几种之间�?
     - 语种相对稳定（如简体中文为主、或繁体中文为主）�?

2. **已知源编码的场景**
   - 典型例子�?
     - 知道某批文件来自 Big5 系统 �?在参数中明确指定源编�?`'950'`�?
     - 知道某批文件来自 GBK 系统 �?在参数中指定 `'GBK'` �?`936`�?
   - 在这种情况下�?
     - 建议**跳过自动检�?*，直接使用用户指定的源编码；
     - 自动检测结果仅作为参考，不参与转换决策�?

3. **统一清理 UTF-8 BOM 残留与常见伪造序�?*
   - 例如�?
     - 清理文本中散落的 `EF BB BF` 内嵌 BOM 片段�?
     - 清理某些错误流程导致�?`C3 AF C2 BB C2 BF` 六字节伪 BOM 序列�?
   - 这类逻辑在自测集中有**专门的用例与回归脚本**，适合�?UTF�?/UTF�? with BOM 场景中安全使用�?

在以上场景中，如遵循后文的“批量操作建议”，一般风险可控制在较低水平�?

## 3. 需人工确认的灰色场�?

以下场景建议�?*仅做检�?预览，不直接批量转换**，或在严格备�?逐步放量的前提下使用�?

1. **短文�?/ 单一内容文件**
   - 如：只有几行配置、只有数�?ASCII 字符的日志片段�?
   - 检测器很难从有限字节中可靠地区�?UTF�? / ANSI / 某些 codepage�?

2. **混合编码文件**
   - 同一文件中，部分内容�?UTF�?，部分是 GBK/Big5 或其他编码；
   - 这类文件本身就不规范，任何“整体转换”都可能产生新的乱码�?

3. **结构未知或疑似包含二进制数据的文�?*
   - 如：可能同时含有文本与图�?压缩片段/自定义二进制块的日志文件�?
   - 对这类文件，建议先用十六进制查看器或专用工具确认结构，再决定是否用文本编码工具处理�?

在以上场景中，推荐的做法是：

- 先对**少量样本**使用转换功能�?
- 使用内置预览或外部编辑器对比“前/后”差异；
- 仅当确认效果良好后，再考虑按相同策略扩展到更多文件�?

## 4. 强烈不建议的使用方式

为了最大程度避免“silent 误伤”，以下使用方式不推荐：

1. **在完全不了解源系�?源编码的情况下，对生产数据整库批量转�?*
   - 例如�?
     - 没有任何背景信息，就对整个项目目录的所有文件做 `-s auto -t utf8 -r`�?
   - 在这种情况下，最好先�?
     - 与业�?系统维护者确认来源编码；
     - 用工具做样本级别的检测和验证�?

2. **忽略预览和备份，直接在原目录覆盖写入**
   - 对重要数据，强烈建议�?
     - 开启“自动备份原始文件”选项�?
     - 或将输出指向单独目录�?

3. **把“猜测到的编码结果”当作事实，连续叠加多轮转换**
   - 例如�?
     - 先将自认为是 GBK 的文件转�?UTF�?�?
     - 再发现仍有问题，又以不同源编码继续转换同一批文件�?
   - 多轮叠加转换极易引入不可逆的字符损坏，应谨慎操作�?

## 5. 安全模式 vs 高级模式（规划中�?

> 本节描述的是计划中的功能设计，用于指导后续开发�?

为平衡“安全性”和“自动化程度”，工具将提供两种整体工作模式：

1. **安全模式（推荐默认）**
   - 检测结果置信度过低或存在多个候选编码时�?
     - 默认仅做“标�?报告”，不直接执行转换；
   - 批量转换时：
     - 强制开启备份功能，或要求输出到新目录；
   - UI 上对高风险操作给予明显提示�?

2. **高级模式（面向熟悉编码问题的专业用户�?*
   - 允许用户显式选择“在低置信场景下仍自动转换”；
   - 启动前需要二次确认，并在设置中以明显方式标记�?
   - 适用于：
     - 用户对数据来源非常熟悉；
     - 已经用样本验证过当前策略�?
     - 需要在脚本/CI 中无人工干预地执行�?

## 6. 推荐使用流程（实践建议）

下面是一条在生产/重要数据场景下推荐的使用流程，帮助你在实际工作中降低风险�?

1. **了解背景**
   - 弄清楚数据来源：系统名称、部署地区、历史编码习惯；
   - 询问业务/运维同事，是否有“已知编码”的确认信息�?

2. **小样本检测与预览**
   - 从目标目录中随机挑选少量代表性文件；
   - 运行检测，查看推断结果与置信度�?
   - 使用预览功能或外部编辑器比较转换前后的文本�?

3. **设置备份与输出策�?*
   - 为首次批量操作开启“自动备份原始文件”；
   - 或者设置输出目录为新的专用路径，保留原文件不动�?

4. **从小范围批量开�?*
   - 先对一小部分文件（例如某个子目录）批量转换�?
   - 检查结果、确�?OK�?
   - 再逐步扩大范围�?

5. **记录配置与策�?*
   - 将使用的源编�?目标编码/置信度阈�?模式（安全或高级）记录下来；
   - 方便后续问题排查与复现�?

6. **在脚�?CI 中使用时的额外注意事�?*
   - 强烈建议在脚本中�?
     - 明确指定源编码（如果已知）；
     - 使用安全模式或保守的置信度阈值；
     - 保留日志与自测结果，便于发现异常�?

## 7. 日志与诊�?

为帮助用户在出现问题时快速定位原因，工具提供了多层次的日志与诊断能力（部分功能规划中）：

- 自测与回归日志：
  - `tests_run.bat` 相关�?`selftest_log`、`perf_log` 等文件，用于验证编码清理与性能�?
- 运行日志�?
  - 记录每次批量转换的输�?输出路径、采用的编码策略、错误与警告�?
- 检测决策日志（规划中）�?
  - �?DEBUG/INFO 级别输出检测步骤和候选列表，帮助高级用户分析检测行为�?

在提交问题反馈（Issue）时，建议同时附上：

- 原始样本文件（或裁剪后的可复现样本）�?
- 转换后的结果文件�?
- 相关日志片段与配置参数（�?目标编码、是否使用安全模式等）�?

---

通过遵循本说明中的建议，你可以在发挥 DeepCharset 编码检测与转换能力的同时，最大程度降低批量操作带来的潜在风险。如在实际使用中遇到疑问或特殊场景，欢迎�?Issue 中描述你的使用背景，我们会根据典型案例持续完善自测集与安全策略�?
